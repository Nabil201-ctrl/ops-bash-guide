# The Ops Bash Guide

Welcome to the **Ops Bash Guide**. This repository contains resources designed to take you from a beginner to building your own cloud infrastructure automation.

## 📂 Contents

- **[`BASH_MASTERY.md`](BASH_MASTERY.md)**: The main course material. **Start reading here.**
- **[`scripts/`](scripts/)**: Practical automation scripts and exercise labs.
    - Automation: `wifi_fixer.sh`, `maintain.sh`, `provision.sh`
    - Challenges: `challenges/` (Broken scripts for debugging practice)

## 🚀 How to Use

1.  Clone the repo:
    ```bash
    git clone https://github.com/Nabil201-ctrl/ops-bash-guide.git
    cd ops-bash-guide
    ```
2.  Follow the levels in `BASH_MASTERY.md`.
3.  Execute the scripts in the `scripts/` directory as you reach the corresponding levels.

## ⚠️ Warning

Some scripts (especially in `scripts/challenges/`) are intentionally broken or dangerous (like `rm -rf` examples in text). **Read the code before you run it.**
